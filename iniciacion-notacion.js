// Notacion

//; --> Delimitar el final de la linea
const b = 4;

b + 4;


// .  --> Se utiliza en los objetos para acceder a los atributos
//movil.anchura

// []  --> Listas, arreglos o arrays
const ar = [1,2,3,4];
console.log(ar[2]);

// () --> Funciones
function suma(a,b){
    // Se escribe la funcion
}

// {} --> Llaves para objetos, funciones y estructura de control
const movil = {
    anchura: 5,
    altura: 10
}

if(true){
    // todo lo que hay entre llaves

    const constantes2 = "hola";
}
